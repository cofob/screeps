export enum LogLevel {
    DEBUG,
    INFO,
    WARN,
    ERROR,
    FATAL,
}

export enum CreepRole {
    harvester = "harvester",
    upgrader = "upgrader",
    builder = "builder",
    repairer = "repairer",
}
